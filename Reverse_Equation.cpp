// Reverse the order of given matematical equation
// In: 20 + x - 65 * y / 10   --->    Out: 10 / y * 65 x + 20

#include <iostream>
using namespace std;

inr main(){

    char str[] = "20+x-65*y/10";
    int n = sizeof(str);

    char out[];

    // for(int i=0; i<n; i++){
    //     if (str[i] == "+" || str[i] == "-" || str[i] == "*" || str[i] == "/"){
    //         out[0] = str[i];
    //     }
    // }

    return 0;
}